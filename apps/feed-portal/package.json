{
	"name": "@r2-event-feed/feed-portal",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "pnpm --filter ./frontend build",
		"dev": "wrangler dev",
		"deploy": "pnpm build && wrangler deploy",
		"test": "vitest run",
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"hono": "^4.10.3"
	}
}
